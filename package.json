{
  "name": "forest",
  "version": "1.0.0",
  "description": "forest Webflow site — JS bundle served via jsDelivr",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
